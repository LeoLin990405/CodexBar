# PR evidence

Screenshots captured from locally packaged CodexBar.app bundles, used as visual proof on pull requests.

## pr3315 — #3235 settings titlebar bleed

Both shots: same fixture, same window, same scroll offset (16 wheel ticks into the General pane),
dark appearance, a freshly built bundle launched with an isolated `HOME` so no account data is shown.
`before` is `upstream/main`; `after` is the PR branch.
